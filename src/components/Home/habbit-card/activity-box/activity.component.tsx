import type React from "react";

const ActivityBox: React.FC<{ userActivity: string[] }> = ({ userActivity }) => {
    return (
        <>
        {userActivity.map(())}
            <div className={}>

            </div>
        </>
    )
}